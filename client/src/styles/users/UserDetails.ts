import styled from 'styled-components';
import { smallBlackText } from '../shared/fontStyles';

export const StyledImage = styled.img`
  border-radius: 50%;
`;

export const StyledName = styled.p`
  ${smallBlackText}
  text-align: center;
`;
