import React from 'react';
import SearchBar from './components/SearchBar';

const App: React.FC = (): JSX.Element => {
  return (
    <div>
      <SearchBar />
    </div>
  );
};

export default App;
